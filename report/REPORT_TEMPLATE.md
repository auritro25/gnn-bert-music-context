# GNN-Based BERT for Understanding Context from Music

> Fill only with real metrics generated by the strict-compliance pipeline. Do not copy illustrative numbers from the assignment brief.

## Abstract
Briefly state the problem, datasets (MusicCaps + FMA), graph construction, BERT/GNN fusion, main quantitative findings, and the strongest conclusion.

## 1. Introduction
- Music context includes genre, mood, instrumentation, structure, and language semantics.
- Sequence-only audio models do not explicitly model repeated/related music segments.
- Research question: does graph structure complement textual context?
- Contributions: segment graph construction; BERT/GNN fusion; strict ablations; leakage-controlled text analysis; cross-modal retrieval.

## 2. Related Work
Cover music tagging, audio representation learning, GNNs for relational music structure, BERT/text encoders, and audio-text retrieval.

## 3. Datasets
### 3.1 MusicCaps
State the exact usable sample count used locally, caption source, labels, deterministic split used for the course experiment, and the limitation that artist identity is unavailable for leakage auditing.

### 3.2 FMA-small
State 8-genre Task-2 benchmark, official split, and artist-disjoint audit result.

### 3.3 FMA-medium
State 16-genre/metadata coverage, top-50 tag target construction from the training split only, official split, and artist-disjoint audit result.

## 4. Preprocessing
- 22,050 Hz
- fixed 5-second FMA segments (10-second MusicCaps clips remain as configured)
- MFCC + chroma node features
- 128-bin log-mel for CNN
- per-track normalization

## 5. Music Graph Construction
Nodes are time segments. Edges are temporal adjacency plus cosine-similarity edges. Report similarity threshold and maximum neighbors from the resolved config.

## 6. Models
### 6.1 CNN baseline
### 6.2 BERT-only baseline
### 6.3 GNN-only GraphSAGE
### 6.4 Early concatenation
### 6.5 Cross-attention GNN-BERT fusion
### 6.6 Contrastive GNN-BERT retrieval

## 7. Experimental Protocol
- label vocabulary from train only
- official FMA train/val/test split
- validation-only checkpoint selection
- validation-only threshold selection for multilabel tasks
- no test-set tuning
- exact target terms masked in leakage-controlled text experiments
- report seed, GPU, epochs, batch size, learning rates

## 8. Results
### 8.1 Task 1 — MusicCaps BERT caption tagging
Insert original and masked-caption results.

### 8.2 Task 2 — FMA-small genre classification
Insert CNN vs GNN accuracy and Macro-F1 plus confusion matrix.

### 8.3 Task 3 — FMA-medium fusion ablation
Insert BERT-only, GNN-only, concat, and cross-attention results using Macro-F1, Micro-F1 and AUC-PR.

### 8.4 Representation analysis
Insert t-SNE and discuss class/tag structure.

### 8.5 Three case studies
Insert graph/text alignment figures and short interpretations.

### 8.6 Task 4 — MusicCaps retrieval
Insert R@1/R@5/R@10 in both directions, ten qualitative retrieval queries, and zero-shot tag-ranking results.

## 9. Discussion
Discuss whether fusion actually beats BERT-only. If it does not, report that directly and analyze text dominance, graph feature limitations, class imbalance, and potential future learned audio node encoders.

## 10. Limitations
Dataset size, MusicCaps audio availability, caption/label semantic overlap, handcrafted graph-node features, compute limits, and generalization.

## 11. Conclusion
Summarize what the experiments actually demonstrate without overstating the result.

## Reproducibility Appendix
Repository structure, environment, exact commands, split audits, configs, checkpoints, and random seed.
